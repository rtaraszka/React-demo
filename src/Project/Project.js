import React from 'react';
import ReactDOM from 'react-dom';

const Project = (props) => {
    return (
        <div>
            <h1>{props.projectName}</h1>
        </div>
    )
};

export default Project